<h1>Juke Box</h1>

<ul>
  <% @bands.each do |band|%>
   <li><%= link_to band.name, band_url(band) %></li>
  <%end%>
</ul>

<%= link_to 'Make a New Band!', new_band_url%><br>
<%= link_to 'Submit a New Album!', new_album_url%><br>
<%= link_to 'Submit a New Track!', new_track_url%><br>
<%= link_to 'Submit a New Song!', new_song_url%><br>

<br>
<br>
<%= link_to 'All The Songs!', songs_url %><br>
<%= link_to 'All The Tracks!', tracks_url %><br>
<%= link_to 'All The Albums!', albums_url %><br>
