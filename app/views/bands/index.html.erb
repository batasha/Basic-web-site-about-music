<h1>Juke Box</h1>

<ul>
  <% @bands.each do |band|%>
   <li><%= link_to band.name, band_url(band) %></li>
  <%end%>
</ul>

<%= link_to 'Make a New Band!', new_band_url%><br>
<%= link_to 'Submit a New Album!', new_album_url%>